import React from 'react'

type Props = {}

const AIResults = (props: Props) => {
  return (
    <div>AIResults
          {/* AI Result Display
          {loading && <p className="text-center mt-4">Processing AI Request...</p>}
          {aiResult && (
              <div className="bg-gray-800 p-6 rounded-md mt-6">
                  <h3 className="text-lg font-bold mb-2">AI Result:</h3>
                  <pre className="text-gray-300 whitespace-pre-wrap">{aiResult}</pre>
              </div>
          )}
      </div>
  ); */}
    </div>
  )
}